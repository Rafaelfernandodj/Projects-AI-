{
  "entities": {
    "School": {
      "title": "School",
      "description": "A school that has students",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "createdAt": { "type": "integer" }
      },
      "required": ["name", "createdAt"]
    },
    "User": {
      "title": "User",
      "description": "A student using Liam",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "email": { "type": "string" },
        "displayName": { "type": "string" },
        "schoolId": { "type": "string" },
        "schoolName": { "type": "string" },
        "level": { "type": "string", "enum": ["Survivor", "Speaker", "Fluent", "Unassigned"] },
        "points": { "type": "integer" },
        "streak": { "type": "integer" },
        "bestStreak": { "type": "integer" },
        "lastActiveDate": { "type": "string" },
        "goal": { "type": "string" },
        "difficulties": { "type": "string" },
        "confidence": { "type": "string" },
        "textSessions": { "type": "integer" },
        "liveSessions": { "type": "integer" },
        "timeSpent": { "type": "integer" },
        "createdAt": { "type": "integer" }
      },
      "required": ["userId", "email", "level", "points", "streak", "bestStreak", "textSessions", "liveSessions", "timeSpent", "createdAt"]
    },
    "Message": {
      "title": "Message",
      "description": "Conversation message",
      "type": "object",
      "properties": {
        "role": { "type": "string", "enum": ["user", "model", "system"] },
        "content": { "type": "string" },
        "timestamp": { "type": "integer" },
        "sessionType": { "type": "string", "enum": ["text", "live"] }
      },
      "required": ["role", "content", "timestamp", "sessionType"]
    },
    "Medal": {
      "title": "Medal",
      "description": "Earned gamification badge",
      "type": "object",
      "properties": {
        "medalId": { "type": "string" },
        "earnedAt": { "type": "integer" }
      },
      "required": ["medalId", "earnedAt"]
    },
    "LiveSession": {
      "title": "LiveSession",
      "description": "Record of a Live Mode voice session",
      "type": "object",
      "properties": {
        "sessionId": { "type": "string" },
        "userId": { "type": "string" },
        "userEmail": { "type": "string" },
        "startedAt": { "type": "integer" },
        "endedAt": { "type": "integer" },
        "status": { "type": "string", "enum": ["active", "completed"] },
        "transcript": { "type": "array" },
        "learnedPhrases": { "type": "array" },
        "mistakes": { "type": "array" },
        "corrections": { "type": "array" },
        "topics": { "type": "array" },
        "summary": { "type": "string" },
        "nextRecommendedStep": { "type": "string" }
      },
      "required": ["sessionId", "userId", "userEmail", "startedAt", "status"]
    },
    "LiveLearningMemory": {
      "title": "LiveLearningMemory",
      "description": "Aggregated learning stats and memory of Liam's Live Mode sessions for a user",
      "type": "object",
      "properties": {
        "lastUpdatedAt": { "type": "integer" },
        "totalLiveSessions": { "type": "integer" },
        "lastSessionAt": { "type": "integer" },
        "currentLevelEstimate": { "type": "string" },
        "mainDifficulties": { "type": "array" },
        "learnedPhrases": { "type": "array" },
        "recurringMistakes": { "type": "array" },
        "strengths": { "type": "array" },
        "preferredLanguageSupport": { "type": "string" },
        "conversationSummary": { "type": "string" },
        "nextRecommendedStep": { "type": "string" }
      },
      "required": ["lastUpdatedAt", "totalLiveSessions", "learnedPhrases"]
    }
  },
  "firestore": {
    "/schools/{schoolId}": {
      "schema": { "$ref": "#/entities/School" },
      "description": "Schools available for users to join"
    },
    "/users/{userId}": {
      "schema": { "$ref": "#/entities/User" },
      "description": "Student profiles and metrics"
    },
    "/users/{userId}/messages/{messageId}": {
      "schema": { "$ref": "#/entities/Message" },
      "description": "History of conversations for a user"
    },
    "/users/{userId}/medals/{medalId}": {
      "schema": { "$ref": "#/entities/Medal" },
      "description": "Medals earned by a user"
    },
    "/users/{userId}/liveSessions/{sessionId}": {
      "schema": { "$ref": "#/entities/LiveSession" },
      "description": "Live sessions for a user"
    },
    "/users/{userId}/memory/{memoryId}": {
      "schema": { "$ref": "#/entities/LiveLearningMemory" },
      "description": "Learning memory status for a user"
    }
  }
}
